<%= render 'shared/description', lead: "Edit #{@tournament.name} tournament", description: nil %>

<%= render 'form', tournament: @tournament, tour: @tour %>
<hr class="my-4">
<%= link_to 'Show', @tournament %> |
<%= link_to 'Back', tournaments_path %>
