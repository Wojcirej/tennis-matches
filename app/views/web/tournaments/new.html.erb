<%= render 'shared/description', lead: "Add new #{@tour} tournament", description: nil %>

<%= render 'form', tournament: @tournament, tour: @tour %>
<hr class="my-4">
<%= link_to 'Back', tournaments_path %>
